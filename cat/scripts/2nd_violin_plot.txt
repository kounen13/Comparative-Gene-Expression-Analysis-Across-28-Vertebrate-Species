ggplot(Cat_Data,
       aes(x=`Anatomical entity name`,
           y=TPM,
           fill=`Anatomical entity name`))+

geom_violin(trim=FALSE)+

scale_y_log10()+

labs(title="Distribution of TPM Across Cat Tissues",
     x="Tissue",
     y="TPM (log10)")+

theme_minimal()+

theme(
axis.text.x=element_text(angle=90,hjust=1),
legend.position="none",
plot.title=element_text(face="bold",hjust=0.5)
)